services:
  - type: web
    name: stocksai
    runtime: python
    buildCommand: pip install -r requirements-render.txt
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
